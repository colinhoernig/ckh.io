box: debian
build:
    steps:
        - arjen/hugo-build:
            version: "0.15"
deploy:
    steps:
        - install-packages:
            packages: git ssh-client
        - git-push:
            gh_oauth: $GIT_TOKEN
            gh_pages: true
            gh_pages_domain: niloc.io
            basedir: public
